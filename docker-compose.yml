services:
  youmeos:
    build:
      context: .
      dockerfile: docker/os/Dockerfile
    image: xengenie/youmeos:latest
    platform: linux/amd64
    container_name: youmeos-microverse
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./wp-content:/var/www/html/wp-content
    environment:
      - PORT=80
      - HTTPS_PORT=443
      - WP_DEBUG=false
